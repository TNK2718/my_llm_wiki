# Phase 4 schema_proposals gold (i127-9285)。
# product_variant が starter に入ったので、Bob ファミリのバリアント関係は
# 既に typed として扱われる。残り weak (「アドオン対象」「イネーブリング・ソフト
# ウェア」) は N < 5 なので自動昇格しない。
schema_version: 1
doc_slug: i127-9285
source: data/eval/sources/i127-9285.md

# 通常 ingest では proposal は出ない (全関係が starter)。
expected_proposals: []

# 通常 ingest では staging には積まれない (全 entity が starter)。
expected_staging: []

# 自動昇格対象なし。アドオン対象 (3 件) / イネーブリング・ソフトウェア (2 件) は
# N=5 未満で weak のまま残る。
expected_weak_promotion: []

canonical_stability:
  max_flips_per_entity_column: 0
